# TricTracZero

Standalone Julia training package for `trictrac_classique`, backed by the Elixir rules engine in the parent repo.

##  [README.md](README.md) Bootstrap

```bash
cd /Users/nick/hermes_trictrac/trictrac_zero
julia --project scripts/bootstrap.jl
```

## Smoke Run

```bash
cd /Users/nick/hermes_trictrac/trictrac_zero
julia --project scripts/smoke.jl
```

## Default Training

```bash
cd /Users/nick/hermes_trictrac/trictrac_zero
julia --project scripts/train.jl
```

## Explore

```bash
cd /Users/nick/hermes_trictrac/trictrac_zero
julia --project scripts/explore.jl
```
